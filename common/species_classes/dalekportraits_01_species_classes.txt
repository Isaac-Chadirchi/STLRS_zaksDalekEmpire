# Author: VelociRandom, Modified by Zak

DALEK = {
	archetype = BIOLOGICAL

	possible = {
		authority = {
			NOT = {
				value = auth_machine_intelligence
				text = SPECIES_CLASS_MUST_NOT_USE_MACHINE_INTELLIGENCE
			}
		}
	}

	graphical_culture = dalekportraits_01
	move_pop_sound_effect = "moving_pop_confirmation"

	trait = trait_kaled_mutant
	trait = trait_dalek_conditioning
	trait = trait_travel_machine
	trait = trait_pathweb
}

